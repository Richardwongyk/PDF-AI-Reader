# PDF AI 阅读器 · 产品需求文档 (PRD)

**版本:** V5.2（2026-07-05 状态校准）
**日期:** 2026-07-05
**作者:** 用户需求深度梳理
**状态:** 持续迭代中

---

## 0. 2026-07-05 状态校准

### 2026-07-05 补充：段落批注/备注

- 新增类似 Word 备注的段落批注能力：用户可在段落右键菜单中选择“批注/备注”，在当前段落后的裂缝区域录入备注。
- 批注裂缝支持展开与收起；收起时不占用阅读空间，仅保留段落侧边黄色标记，避免打断主阅读流。
- 当同一段落已有翻译裂缝时，批注会插入在翻译之后；当先写批注再打开翻译时，翻译会保持在批注之前，确保“原文 -> 翻译 -> 备注”的阅读顺序。
- 批注内容按文档 hash 与 block id 持久化到本地 `data/annotations.json`，不上传云端；清空批注会同步移除本地记录和侧边标记。
- 大文档稳定性补充：导入长文档时自动页级公式扫描不再全页排队，默认只处理当前视口附近页；后台页面 blocks 与目录树刷新改为按需/防抖，降低 Qt 原生层崩溃风险。

本 PRD 仍描述产品目标，但早期“本地 Qwen/BGE-M3 默认、两阶段启发式公式识别、Chroma 单一路线”的表述已经不再代表当前主线。当前实现以本地 PDF 结构解析、异步多轮公式索引、可配置云端/本地模型、SQLite FTS/Chroma 可切换知识库和可审计 accepted 写回为核心。

当前必须明确的边界：

- born-digital PDF 公式默认不 OCR，优先使用文本层、glyph、font、bbox、vector 和 r0/r0.5 结构证据。
- OCR/MFR 只用于图片、扫描、无文本层、乱码/缺映射、低置信或用户显式精扫。
- 公式候选、云端复核和本地工具结果默认 candidate-only；只有 accepted 结果才允许进入知识库和 GraphRAG。
- 基础公式审核 UI、manual revision、evidence 预览、PDF bbox 定位和 r5 增量写回已经接线；批量审核和最终高精度公式还原仍未完成。
- Attention/Napkin 是当前验收基准；Napkin 公式质量门禁仍失败，不能宣称 99.9% 公式还原达成。
- 2026-05-28 性能修复尝试暴露新的 P0 体验问题：Napkin 极大缩放/快速滚动/跳页时可能出现黑底或空白页。
  这违反阅读器核心需求。后续性能优化必须先保证页面内容始终可见，再谈渲染耗时指标。
- 翻译链路仍需专项修复：公式占位保护必须每次请求隔离，避免并发翻译互相污染公式；翻译预处理不应
  用手写规则猜测或改写无证据公式。
- 2026-06-05 已完成一组 UI 小修并提交：左右侧栏隐藏/显示按钮位于菜单栏同层两角；左右 dock
  不可关闭但可弹出/归位；右侧 `AI 工具集` 是全文问答、证据、回答和追问入口，不再只是占位。
  PDF 阅读区保留按需横向滚动，侧栏折叠后会水平居中，证据 bbox 跳转会同步横向定位。
- 翻译裂缝会按原文 block bbox 设置正文左右 padding；开过翻译框的 split page 在缩放或离屏恢复后
  会按当前 page meta 宽度重建，避免停留在旧缩放宽度。
- 菜单栏文字区域双击折叠尝试过但未可靠满足需求，当前暂不继续；多文档标签页/标签关闭当前文档
  也已讨论，当前暂不实现。
- 最新已补跑 `tests/test_smoke.py tests/test_pdf_viewer_navigation.py -q`：30 passed。
  这只是 UI/导航单元级回归，不等同于 Attention/Napkin 桌面 E2E 或公式质量门禁通过。
- 2026-07-05 当前 HEAD 已快进到 GitHub `origin/master` 的 `915d5f5`；
  本地协作/交接文档已从版本库移出并由
  `.gitignore` 管理，但仍是新会话接手入口。
- 2026-07-05 用户确认保留当前未提交 TinyBDMath Graph Parser M5 工作树改动：
  whole-formula graph context、结构化关系冲突筛选、默认批量 torch eval 和快速评估
  可跳过 layout verifier。这是公式恢复候选质量实验，不代表公式 accepted gate 已打开，
  也不代表 Napkin 99.9% 质量目标达成。
- 2026-07-05 当前验证：新增批注/TOC/运行时检查 27 passed；
  合并回归 + M5 定向组合 94 passed；轻量接手测试 95 passed；
  TinyBDMath 主线测试 159 passed。
- 2026-07-05 已移除第三方公式工具 worker/对比通道；后续公式恢复主线只保留
  本机 TinyBDMath 结构模型和旧图片公式 cache/OCR fallback，不再发现或调用
  `pdf_tool_*` 隔离工具环境。

## 1. 引言

### 1.1 项目名称
**PDF AI 阅读器** (英文: PDF AI Reader)

### 1.2 产品目标
本软件旨在成为专业读者的终极研究助手，彻底改变"阅读-翻译-理解"的割裂流程。它以 **"AI 系统化内化全文 → 本地知识库存储 → 精确对照翻译 → 沉浸式裂缝交互问答"** 为闭环，集成英译中、公式解读、AI 沉浸式问答交互等功能，帮助理学研究者以母语思维直接穿透英文论文的认知壁垒。核心不是翻译器，而是**以论文为单位的 AI 内化引擎**。

### 1.3 目标用户
- **主要用户**：数学、物理、计算机、人工智能、机器学习等领域的本科高年级学生、硕博研究生、科研人员、高校教师。
- **典型场景**：精读领域奠基性论文（如 "Attention Is All You Need"）、啃读数百页英文专业书籍、快速掌握某篇论文的证明脉络、撰写文献综述时对照提取关键论点。

### 1.4 核心价值
- **从"机翻"到"内化"的范式升级**：AI 不仅翻译，而是先批量深度解析全文，建立向量知识库。此后每一次提问、每一次翻译，都基于对全文的融会贯通，而非孤立句子。
- **"裂缝式"零干扰交互**：独创动态嵌入界面，在原文段落间直接"裂开"交互区，翻译、解释、问答就地呈现，彻底杜绝传统侧边栏或弹窗造成的视焦跳转与原文失联。
- **本地解析与可配置模型路由**：PDF 解析、结构证据、缓存和知识库可在本地完成；生成模型支持 LiteLLM 云端和 Ollama 本地后端。云端调用必须由配置和 UI 状态清晰告知，不能把云端伪装成本地。
- **专业语境铁壁守护**：数学公式绝对原样保留，定理、证明等结构获得特殊保护；强制术语表与翻译风格工程化，杜绝"不人话"直译，达到信达雅。

### 1.5 当前技术环境

| 环境 | 版本/配置 |
|------|----------|
| Python | 3.14.4 (2026 LTS) |
| GUI 框架 | PySide6 6.11.0 |
| Conda 环境 | `pdf_ai_reader_314` |
| 生成模型 | LiteLLM 云端 / Ollama 本地 / Mock 测试降级 |
| 检索后端 | SQLite FTS5 / Chroma / LlamaIndex 编排预留 |
| PDF 引擎 | PyMuPDF 1.27+ |
| 知识库 | `KnowledgeEngine` facade，按配置和 embedding 可用性选择后端 |
| 公式路线 | r0/r0.5/r1/r2/r2a/r3/r4/r5 异步多轮候选与 accepted 写回 |

---

## 2. 功能需求详述

### 2.1 文档处理与解析

| 编号 | 功能 | 详细描述 | 状态 |
|------|------|---------|------|
| F-1.1 | PDF 打开与高性能渲染 | 支持菜单和文件对话框打开本地 PDF。渲染基于 PyMuPDF、虚拟布局和懒加载页面 widget。当前已补按需横向滚动、侧栏折叠后水平居中、bbox 横向定位、大页面 fallback 和 split page 缩放重建路径；但 2026-05-28 Napkin 400x 前台失败仍需重新做桌面 E2E 验证后才能关闭。 | 🟡 已实现基础能力，极端 Napkin E2E 待复测 |
| F-1.2 | 段落智能分割 | 打开文档后自动后台分析，基于 PyMuPDF 文本块坐标和排版特征聚合为自然段。每个段落作为独立可点击单元，支持双栏 PDF 交错排列。右键段落可触发翻译/提问/解释。 | ✅ 已实现 |
| F-1.3 | 公式候选与证据链 | born-digital PDF 默认走 r0/r0.5 结构事实和 TinyBDMath 本机结构模型，抽取文本层、glyph、font、bbox、vector、图片和风险证据；第三方公式工具 worker 已移除。低置信结果不可覆盖正文，accepted 后才进入知识库。 | 🟡 多轮候选已实现，最终高精度未达标 |
| F-1.4 | 图表与表格识别 | 检测页面内图像区域和表格线条，提取包围框坐标。PyMuPDF 自动将非文本块标记为 IMAGE 类型。 | 🟡 基础版 |
| F-1.5 | 原文排版完整性 | 提取的文本保留原始换行符和缩进，公式在翻译展示时通过占位符保护后恢复。 | ✅ 已实现 |
| F-1.6 | 扫描版 PDF 支持 | 规划中。将集成 OCR 引擎处理扫描页。 | ⏳ 规划 |

### 2.2 AI 全文理解与知识库构建

| 编号 | 功能 | 详细描述 | 状态 |
|------|------|---------|------|
| F-2.1 | 批量预处理流水线 | 打开 PDF 后自动执行页面解析、块分割、基础索引和多轮公式任务入库。知识库后端可用 SQLite FTS5/Chroma 等；同一文档按指纹和 input hash 跳过未变化任务。 | ✅ 已实现 |
| F-2.2 | 精细颗粒度知识块 | 每个知识块（DocumentBlock）包含：唯一 ID（`p4_b12`）、页码、块类型（paragraph/formula/heading/image）、原始内容、包围框坐标、章节标题、扩展元数据。 | ✅ 已实现 |
| F-2.3 | 检索问答 | `KnowledgeEngine` 返回带页码、bbox、block id、词面/向量分数的 evidence。无真实 embedding 时优先 FTS5 快速召回，真实语义质量仍依赖向量后端/重排/云端 reasoning。 | ✅ 基础已实现 |
| F-2.4 | 智能摘要与内容标注 | 章节标题提取 + 定理/定义环境标记（检测 "theorem", "lemma", "proof" 等关键词）。 | 🟡 基础版 |
| F-2.5 | 知识库生命周期管理 | 菜单"工具-构建/重建知识库"支持强制重建。删除和空间管理待 UI 完善。 | 🟡 基础版 |

### 2.3 翻译功能

| 编号 | 功能 | 详细描述 | 状态 |
|------|------|---------|------|
| F-3.1 | 段落翻译 | 右键段落或双击段落触发翻译。在当前段落下方的裂缝容器中插入译文，Markdown + LaTeX 实时渲染。裂缝宽度跟随页面，正文左右 padding 按原段落 bbox 对齐。 | ✅ 已实现 |
| F-3.2 | 公式绝对保护 | 翻译前将 LaTeX 公式替换为占位符 `【FORMULA_N】`，System Prompt 强制要求保留占位符。译后反向替换，JavaScript 端再走独立 KaTeX 渲染。当前需要修复并发流式翻译共享占位 store 的风险，并尽量原样保留公式 delimiter/内容。 | 🟡 基础已实现，并发隔离待修 |
| F-3.3 | 术语强制翻译 | 翻译 Prompt 自动注入全部术语表（英文→中文映射列表），用户可设置 force 标志强制执行。支持 math/cs_ml/physics 三学科术语包。 | ✅ 已实现 |
| F-3.4 | 定理/证明环境保护 | 检测关键词（theorem, lemma, proof, definition）后，System Prompt 追加指令："必须保持条件与结论精确对应，严禁增删逻辑连接词"。 | ✅ 已实现 |
| F-3.5 | 翻译风格工程化 | 三层控制：(1) System Prompt 预定义风格指令；(2) Few-shot 示例（2 组英中对照）；(3) LaTeX 格式强制（`\(...\)` / `\[...\]`）。 | ✅ 已实现 |
| F-3.6 | 混合模型路由 | 翻译任务支持 local_first / cloud_only / local_only 三种路由策略。自动检测模型可用性并降级（本地不可用→云端→Mock）。 | ✅ 已实现 |
| F-3.7 | 取词翻译 | 规划中。鼠标悬停英文单词显示浮动气泡释义。 | ⏳ 规划 |
| F-3.8 | 逐句对照翻译 | 规划中。将段落按句号拆分后逐句翻译，以两列对照展示。 | ⏳ 规划 |

### 2.4 裂缝式交互（核心交互）

裂缝是软件的**灵魂**——它不是弹窗，而是直接插入到 PDF 内容流中的临时动态交互容器。

#### 2.4.1 裂缝形态
- **展开态**：全宽视图，蓝紫渐变背景 + 圆角边框。包含标题栏、上下文指示器、输入区（提问模式）、QWebEngineView 结果区（Markdown + LaTeX 渲染）、追问建议按钮、操作栏（复制/重新生成/清除）。
- **折叠态**：完全隐藏——页面上下段无缝合并，段落左侧显示淡蓝色翻译指示条。
- **切换方式**：双击裂缝自身 / 点击“折叠”按钮 / 按 Esc 键。

#### 2.4.2 触发机制
- 双击段落 → 首次翻译，再次折叠/展开切换
- 右键段落 → "翻译段落" / "批注/备注"。暂不在段落右键菜单中展示未完整打磨的提问/解释入口。

#### 2.4.3 交互行为
- 翻译模式：直接显示译文（不显示输入框）
- 提问模式：显示输入框 → 支持 Ctrl+Enter 发送 → 流式显示回答
- 追问建议：回答完成后自动生成 3 个可点击追问问题
- 多轮对话：保留最近 6 轮对话历史
- 底部拖拽手柄：可手动调整裂缝高度（拖拽后锁定自动高度）
- 自动撑高：内容超出当前高度时自动扩展（当前上限 800px）

| 编号 | 功能 | 状态 |
|------|------|------|
| F-4.1 | 裂缝展开/折叠（下方模式） | ✅ 已实现 |
| F-4.2 | 翻译模式 | ✅ 已实现 |
| F-4.3 | 提问模式（含知识库检索） | ✅ 已实现 |
| F-4.4 | 解释模式 | ✅ 已实现 |
| F-4.5 | 流式显示 + Markdown/LaTeX 渲染 | ✅ 已实现 |
| F-4.6 | 追问建议 | ✅ 已实现 |
| F-4.7 | 多轮对话 | ✅ 已实现 |
| F-4.8 | 拖拽调整高度 | ✅ 已实现 |
| F-4.9 | 右侧分栏模式 | ⏳ 规划 |
| F-4.10 | 裂缝展开/折叠动画 | ✅ 已实现基础动画 |
| F-4.11 | 段落批注/备注 | ✅ 已实现。右键段落选择“📝 批注/备注”后，在段落关联裂缝中保存本地备注；批注框提供显式“折叠”按钮，可展开查看/编辑，也可收起为黄色侧边标记，不影响连续阅读。批注 JSON 写入采用跨进程锁和单条 patch 保存，多窗口修改同一文档的不同段落不会互相覆盖；暂不做实时推送，关闭重开后读取最新持久化结果。 |

### 2.5 术语表管理

| 编号 | 功能 | 详细描述 | 状态 |
|------|------|---------|------|
| F-5.1 | 内置学科术语包 | 随软件分发三个 JSON 文件：math.json (73条)、cs_ml.json (70条)、physics.json (45条)。每条含 en/zh/domain/force/aliases/notes 字段。 | ✅ 已实现 |
| F-5.2 | 术语强制与冲突解决 | force=True 的条目优先翻译；多义词（如 "kernel"→数学"核"/CS"内核"）按优先域选择。 | ✅ 已实现 |
| F-5.3 | 导入外部术语表 | 支持 JSON 和 CSV 格式导入，存入 "imported" 领域。 | ✅ 已实现 |
| F-5.4 | 术语表编辑器 | 图形化编辑器（计划中）。当前通过菜单提供占位提示。 | ⏳ 规划 |

### 2.6 导航与书签

| 编号 | 功能 | 详细描述 | 状态 |
|------|------|---------|------|
| F-6.1 | 目录导航面板 | 左侧 Dock 显示树形目录。优先使用 PDF 原生大纲；无大纲时先从结构化标题模式（如“第X节”“一、”“1、”）推断目录，再回退到 heading 块，并过滤乱码、公式碎片、空标题、重复标题和过少的低置信目录候选。无法形成可靠目录时显示为空，不保留上一份文档的目录。点击跳转。 | ✅ 已实现 |
| F-6.2 | 手动书签 | 导航器支持书签增删改查和排序。UI 集成为树形视图。 | ✅ 已实现 (Core层) |
| F-6.3 | AI 智能书签建议 | 自动检测 heading 块和 theorem/lemma/definition 块，建议为书签。 | ✅ 已实现 (Core层) |

### 2.7 界面与主题

- **主题**：三套完整 QPalette 主题——素白（学术）、暗夜、护眼羊皮纸。通过 `QApplication.setPalette()` 全局应用。
- **布局**：顶部菜单栏 + 工具栏 → 左侧目录 Dock + 中央 PdfViewer + 右侧 AI 工具集 Dock → 底部状态栏（页码、模型状态、进度条）。
- **操作**：菜单栏（文件/视图/工具/帮助），工具栏（打开/搜索/页码/公式精扫），菜单栏同层左右角侧栏按钮，快捷键（Ctrl+O 打开，Ctrl+Enter 发送问题）。

| 编号 | 功能 | 状态 |
|------|------|------|
| F-7.1 | 菜单栏 + 工具栏 + 状态栏 | ✅ 已实现 |
| F-7.2 | 左侧目录导航 Dock | ✅ 已实现 |
| F-7.3 | 右侧 AI 工具集 Dock | ✅ 已实现全文问答/证据/回答/追问基础入口 |
| F-7.4 | 三套主题 | ✅ 已实现 |
| F-7.5 | 设置对话框（云端 API 配置） | 🟡 简化版 |
| F-7.6 | 完整设置对话框 | ⏳ 规划 |
| F-7.7 | 左右侧栏隐藏/显示与 dock 弹出/归位 | ✅ 已实现 |
| F-7.8 | 多进程多窗口共存 | ✅ 最小版。允许同时启动多个独立 PDF AI Reader 进程窗口；每个进程使用独立日志和 QtWebEngine runtime 目录，secondary 实例自动避开 ChromaDB 并使用 SQLite FTS 知识库后端。 |

---

## 3. 非功能需求

### 3.1 性能（实测）
- 百页文档任意滚动无卡顿（虚拟视口懒加载）
- 冷启动至主界面可见：≤2 秒
- 百页文档首屏解析至可交互：≤3 秒（阶段一启发式分块）
- ML 公式精扫：后台异步，不阻塞阅读
- 知识库构建：FTS5 可在长文档上秒级建立词法索引；真实向量后端按 embedding 可用性和后端配置决定。
- 内存占用：默认阅读路径不得加载 OCR/MFR 或训练模型；第三方公式工具 worker 已移除，显式精扫也只能写候选。

### 3.2 可用性
- 核心翻译和提问操作均不超过两次点击（右键 → 选择）
- 模型状态必须清晰展示云端/本地/Mock 路由，不能把不可用本地模型作为启动阻塞项
- 错误处理：网络/模型失败提供清晰提示

### 3.3 稳定性与可维护性
- 全局异常钩子：未捕获异常写入日志，弹简化错误对话框
- 日志系统：所有 AI 调用和耗时记录到进程级 `logs/app-<pid>.log`，避免多个窗口同时写同一日志文件；启动终端默认不输出 INFO 级日志，避免 Windows/Conda 控制台编码导致中文日志乱码。需要调试时可设置 `PDF_AI_READER_CONSOLE_LOG=1`。
- 模块化三层架构（ui/core/data），依赖注入

### 3.4 安全与隐私
- 本地解析、缓存和索引不需要上传 PDF；配置云端 API Key 后，翻译/问答/r3 复核可能发送必要上下文，UI 和配置必须明确提示。
- API Key 存储在 `config.yaml`（可通过 .env 替代）
- 本地 Ollama 通信仅通过 `localhost:11434`

### 3.5 可移植性
- 当前目标平台：Windows 10/11（通过 conda 环境）
- macOS / Linux 计划后续支持

---

## 4. 数据需求
- **知识库**：ChromaDB 持久化至 `data/knowledge_bases/`，每个文档一个 Collection
- **术语表**：JSON 格式，结构 `{"domain": "...", "terms": [{"en":"...","zh":"...","force":true}]}`
- **配置**：`config.yaml`，支持 GUI 修改（简化版）和手工编辑
- **日志**：`logs/app.log`，含 AI 调用耗时和错误堆栈

## 5. 接口需求
- **LLM 抽象层**：`BaseLLMClient.generate(messages) → str` 和 `generate_stream(messages) → Generator[str]`
- **OllamaClient**：本地 REST API 客户端
- **LiteLLMClient**：云端统一客户端（支持 100+ 模型，内置 3 次自动重试）
- **MockLLMClient**：测试/降级用模拟客户端
- **HybridModelRouter**：任务路由决策
- **TranslationService**：公式保护 → 术语注入 → 风格控制 → LLM 调用 → 公式恢复
- **QAService**：知识库检索 → 上下文组装 → 多轮对话 → LLM 调用

---

## 6. 版本规划

| 里程碑 | 版本 | 核心交付物 | 状态 |
|--------|------|-----------|------|
| M0 核心原型 | V0.5 | PDF 渲染、段落/公式分割、裂缝交互、本地翻译、基础菜单 | ✅ 已完成 |
| M1 知识库集成 | V0.7 | ChromaDB 集成、知识库构建、虚拟视口懒加载 | ✅ 已完成 |
| M2 功能完善 | V1.0 | 术语表管理、混合路由、智能书签、公式 ML 精扫、主题系统、检索增强问答 | 🟡 进行中 |
| M3 正式版 | V1.5+ | 取词翻译、逐句对照、右侧分栏、OCR、图表理解、跨平台 | ⏳ 规划 |

---

*本 PRD 文档已按 2026-07-05 当前实现校准。公式最终质量、批量审核、极端 Napkin 桌面 E2E 和产品级 GraphRAG 仍是未完成项。*
